#universal functions
#ufunc
#operate on every element automatically
import numpy as np

arr = np.array([1, 4, 9, 16])

#square root
print(np.sqrt(arr))

#exponential
print(np.exp(arr))

#trigonometric function
#sine

angles = np.array([0, np.pi/2])
print(np.sin(angles))
